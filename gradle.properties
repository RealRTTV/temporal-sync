# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = v1_03
maven_group = ca.rttv
archives_base_name = temporal_sync

# Dependencies are managed at gradle/libs.versions.toml
